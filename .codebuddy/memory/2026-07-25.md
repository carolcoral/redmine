# 2026-07-25

## CNB Git 令牌配置
- 远程仓库: https://cnb.cool/xindu.site/redmine
- 用户名 cnb，令牌名 codebuddy，令牌 1u1d2fT7qblV29Ijythdlp3DaLB
- 凭证存储: .git/.git-credentials（本地） + macOS Keychain
- 旧 Keychain 令牌 6jxxxx 过期，已清除并写入新令牌

## 插件嵌套 git 仓库修复
- 19 个插件目录含独立 .git，redmine_calendar 为空仓库导致 git add 失败
- 已移除全部 plugins/*/.git，插件作为普通目录纳入主仓库

## 数据库位置
- config/database.yml（PostgreSQL: 172.24.108.92:5432, redmine_dev/redmine_dev_test）

## Redmine 集群部署配置（环境变量驱动，默认关闭）
- config/environments/production.rb: 加入注释版 Redis/Memcached cache_store（REDIS_URL / MEMCACHE_SERVERS 驱动），默认 Rails 原生缓存
- config/configuration.yml production 段: 加入注释版附件共享存储（NFS 共享目录 或 S3 云存储，ATTACHMENTS_STORAGE_TYPE/S3_* 环境变量），默认本地 filesystem
- 会话: Rails 加密 Cookie Store，集群安全；需各节点 secret_key_base 一致
