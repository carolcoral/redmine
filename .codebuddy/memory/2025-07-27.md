# 2025-07-27

## 添加管理员自定义系统提示词功能
- 在 `init.rb` 新增 `system_prompt` 设置项（默认空字符串）
- 设置页面添加多行文本域，管理员可输入自定义提示词控制 AI 行为边界
- `chat_controller.rb#build_system_content`：自定义提示词注入到 system prompt 最前面（优先级最高）
- `report_generator.rb#build_system_prompt`：同样注入自定义提示词
- 修复 `guard_prompt_enabled` 设置未被使用的问题：chat_controller 中新增开关检查 `guard_enabled != '0'`
- 添加中英文翻译：`label_system_prompt`、`text_system_prompt_hint`、`text_system_prompt_placeholder`
