# 2026-07-27

## Bug 修复
- **AVOID eager loading: Issue => [:author]**: `chat_controller.rb:169` 的 `.includes` 中 `:author` 未被使用，已移除
- **AVOID eager loading: Issue => [:priority, :tracker]**: 2处修复
  - `issue_query.rb:413`: 无条件 `preload(:priority)` 改为按 columns 条件加载
  - `redmine_indicator/_my_issues_summary.html.erb`: column_names 从 `["project","tracker","status","subject"]` 改为 `["id"]`（partial 只计数不展示）
- **Attachment.new(:file => ...) NoMethodError**: `file=` 被 `private` 盖住（第132行），在 `file=` 前加了 `public`
- **thumbnail 图片无法预览**: 2个 bug 修复
  - `cloud_download_tempfile` 末尾多余的 `true` 导致返回错误值
  - `with_content_tempfile` 在文件不可读时不抛异常，导致 thumbnail 返回未生成的路径

## redmine_ai_assistant 插件改动
- **兼容 Redmine 6.1.2**: `init.rb` requires_redmine 改为 6.1.0，2 个 migration 的 Migration[7.0] 改为 [6.1]
- **作者改为 carolcoral**
- **新增自动获取模型功能**: 
  - `AiProvider#models_endpoint` 和 `fetch_remote_models` 方法
  - `ProvidersController#fetch_models` action
  - `POST /ai_providers/fetch_models` 路由
  - 表单中"自动获取"按钮 + JS（AJAX POST 含 CSRF token）
  - 中英文 i18n 翻译（按钮、状态、错误提示）
  - CSS 样式
- **自定义悬浮图标上传**: 设置页新增文件上传字段，JS 用 FileReader 转 base64 填入 URL 字段；widget 头部头像、欢迎页图标、AI 消息头像全部同步使用自定义图标；更新 CSS 适配图片头像；补充 i18n
- **AI 对话框欢迎消息重复**: `_widget.html.erb` 中静态欢迎消息与 JS `showWelcomeMessage()` 重复渲染；改为由 JS 统一生成完整欢迎消息（含快捷问题按钮、免责声明），通过 data 属性传递 i18n 文案；快捷问题按钮改为事件委托，支持动态创建的欢迎页

